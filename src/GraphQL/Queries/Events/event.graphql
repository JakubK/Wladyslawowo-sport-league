query event($id: Int!)
{
  event(id:$id)
  {
    id,
    name,
    description,
    medias,
    date,
    players:scores{
      id,
      points,
      player{
        name,
        settlement
      }
    }
  }
}