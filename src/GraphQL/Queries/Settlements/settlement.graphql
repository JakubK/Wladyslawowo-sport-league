query settlement($id: Int!)
{
  settlement(id:$id)
  {
    name,
    media,
    description,
    players{
      name,
      points,
      scores{
        event{
          name,
          date,
          season
        },
        points
      }
    }
  }
}