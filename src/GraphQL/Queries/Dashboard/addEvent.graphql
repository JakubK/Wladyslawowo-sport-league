mutation addEvent($name: String!, $description: String, $date: Date, $season: Int, $scores: [AddScore], $links: [String])
{
    addEvent(name: $name, description: $description, date: $date,season: $season, scores: $scores, links: $links)
    {
        id
    }
}