query event($id: Int!)
{
  dashboardEvent: event(id: $id)
  {
    id,
    name,
    description,
    date,
    season,
    medias,
    scores
    {
      playerId,
      name: playerName,
      points
    }
  }
}