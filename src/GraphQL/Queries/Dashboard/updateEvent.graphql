mutation updateEvent($id: Int!, $name: String, $description: String, $date: Date, $scores: [AddScore])
{
  updateEvent(id: $id, name: $name, description: $description, date: $date, scores: $scores)
  {
    id
  }
}